`#include <iostream>`
- cout 객체 선언을 위한 iostream 해더파일
  
`using namespace std;`
- cout를 간략하게 사용하기 위해 std 네임스페이스를 사용
  
`int main() {`
- 메인 함수 시작
  
`for (int a = 0; a < 4; a++) {`
- 변수 a을 선언 후 0으로 초기화 후 3값이 될 때까지 반복
  
`for (int b = 0; b <= a; b++) {`
- 변수 b을 선언 후 0으로 초기화 후 b값이 될 때까지 반복
  
`cout << "*";}`
- 화면에 다음과 같은 텍스트 출력
  
`cout << "\n";}`
- 화면에 다음과 같은 텍스트 출력
  
`return 0;`
- 0을 반환 후 메인 함수 종료
