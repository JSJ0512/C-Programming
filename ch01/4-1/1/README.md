- 배열명은 배열의 첫 번째 요소의 주소를 가리키는 상수 포인터이며 연속된 메모리공간을 식별하는 역할을 함

- 객체가 메모리에 위치한 시작 주소값을 저장하는 변수를 의미함

- int *ptr = arr 와 같은 포인터 변수를 선언하고 &arr[i] 같은 임의의 정수로 주소값을 반환 할 수 있음

- int *ptr = arr 와 같은 포인터 변수를 선언하고 *(ptr + i) 를 사용하여 arr[i]로 값을 얻을 수 있음
