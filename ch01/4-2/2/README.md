- NULL은 포인터가 유효한 메모리 주소를 가리키고 있지 않음을 나타내는 특수한 상수 값임 , C에서는 (void*)0, C++에서는 0으로 정의됨

- 널문자 ('\0')은 문자형 데이터 char 이며 문자열 끝을 표시하는 종료 문자로 모든 C 문자열은 반드시 '\0'로 끝남 , NULL은 포인터가 아무 것도 가리키지 않음을 의미하며 주소가 없는 상태 표현함

- nullptr은 포인터가 아무것도 가리키지 않음을 나타내는 널 포인터 리터럴로, NULL이나 0의 문제를 해결하기 위한 안전성을 위해 도입된 타입임
