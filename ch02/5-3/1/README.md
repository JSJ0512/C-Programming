- 메모리 사용량 측면에서는 데이터 복사본을 생성하지 않고 메모리 주소만을 전달하므로 메모리 사용량을 절약할 수 있음 , 처리 시간 측면에서는 복사 시간을 제거하여 데이터를 복사하는 것에 소요되는 시간이 발생하지 않음

- return 실행 시 반환값을 레지스터 또는 임시 메모리에 저장하며 반환값 저장을 함 - > 함수의 지역 변수와 객체가 소멸되고 스택 메모리가 해제되 지역 변수 정리를 함 - > 저장된 복귀 주소를 이용해 호출한 위치로 돌아가며 제어권 복귀를 함 - > 저장해둔 반환값이 호출자의 변수에 전달되며 호출부에서 값 수신을 함

- 변수의 복사본이 아닌 원본 메모리 공간을 직접 반환하는 것임

- 값이 1,2,3으로 1씩 증가되며 cout 2번째 3번째 줄의 ref와ref2를 전부 n으로 바꾸어도 n에 대한 참조 리턴을 하였기 때문에 정상 작동을 

코드 <img width="432" height="333" alt="image" src="https://github.com/user-attachments/assets/d6c9b232-9e9e-4d61-90b1-1fef241e6450" />

결과 <img width="97" height="51" alt="image" src="https://github.com/user-attachments/assets/8fdf7e48-9f54-4ce7-9de4-67a4fb9776a7" />

